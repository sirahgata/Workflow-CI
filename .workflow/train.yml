# Workflow CI reference for Kriteria 3 (Basic)
# GitHub Actions aktif: .github/workflows/train.yml

name: Train ML Model
trigger:
  - push
  - workflow_dispatch
steps:
  - setup Python 3.12
  - install mlflow==2.19.0
  - mlflow run MLProject --env-manager local
